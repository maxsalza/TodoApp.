# TodoApp.
step 1 run the script to generate the database (./database/todoapp.sql)
step 2 run the command to start backend (cd ./TODOAPP/server) (npm start )
step 3 run the command to start backend (cd ./TODOAPP/client) (npm start )
